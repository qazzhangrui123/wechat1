<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/ripple_bg">

    <ImageView
        android:id="@+id/contact_recycler_image"
        android:layout_width="@dimen/chat_recycler_image_size"
        android:layout_height="@dimen/chat_recycler_image_size"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="5dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:src="@drawable/ic_launcher_background"/>

    <TextView
        android:id="@+id/contact_recycler_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/contact_recycler_name_margin"
        android:layout_marginEnd="@dimen/contact_recycler_name_margin"
        android:text="@string/chat_list_recycler_user"
        android:textSize="@dimen/contact_recycler_name_size"
        android:textColor="#000000"
        android:lines="1"
        android:ellipsize="end"/>

</LinearLayout>